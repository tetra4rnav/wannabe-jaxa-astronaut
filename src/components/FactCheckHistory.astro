---
import { FactCheckHistoryClient } from './FactCheckHistoryClient';

const id = Astro.locals.starlightRoute?.id ?? '';
const skip = !id || id === 'index' || id === 'about' || id.startsWith('_template');
---

{!skip && <FactCheckHistoryClient client:load docsId={id} />}
